# Codebase Structure

**Analysis Date:** 2026-04-25

## Directory Layout

```
ttsQwen/
├── .git/                        # Git repository metadata
├── .opencode/                   # OpenCode AI tooling configuration
│   ├── .gitignore               # Ignores node_modules, package files from git
│   ├── node_modules/            # OpenCode plugin dependencies (gitignored)
│   ├── package.json             # OpenCode plugin dependency manifest
│   ├── package-lock.json        # Dependency lockfile
│   └── plugins/                 # Custom OpenCode plugins
│       └── graphify.js          # Knowledge graph reminder plugin
├── .planning/                   # GSD workflow planning artifacts
│   └── codebase/                # Codebase analysis documents (this file)
├── AGENTS.md                    # AI agent instructions (graphify rules)
└── opencode.json                # OpenCode CLI configuration
```

## Directory Purposes

**`.opencode/`:**
- Purpose: Houses OpenCode AI development tool configuration and plugins
- Contains: Plugin source code, npm dependencies, gitignore for generated files
- Key files: `.opencode/plugins/graphify.js` (knowledge graph plugin)
- Note: This directory is largely gitignored — only `plugins/` should be committed

**`.opencode/plugins/`:**
- Purpose: Custom OpenCode plugins that hook into AI tool lifecycle
- Contains: JavaScript modules exporting async plugin factories
- Key files: `graphify.js` — injects graphify knowledge graph reminders

**`.planning/`:**
- Purpose: GSD (Getting Stuff Done) workflow artifacts
- Contains: Phase plans, codebase analysis, project configuration
- Key files: Documents generated by `/gsd-map-codebase` and other GSD commands
- Generated: Yes (by GSD tooling)
- Committed: Yes

**`.planning/codebase/`:**
- Purpose: Codebase analysis documents consumed by GSD planning and execution
- Contains: ARCHITECTURE.md, STRUCTURE.md, STACK.md, CONVENTIONS.md, etc.

## Key File Locations

**Configuration:**
- `opencode.json`: OpenCode CLI configuration — declares which plugins to load
- `.opencode/package.json`: npm manifest for OpenCode plugin dependencies (`@opencode-ai/plugin` v1.14.20)
- `.opencode/.gitignore`: Prevents committing generated/dependency files from `.opencode/`

**Agent Instructions:**
- `AGENTS.md`: Rules for AI agents operating on this project (currently graphify rules only)

**Development Tooling:**
- `.opencode/plugins/graphify.js`: Knowledge graph reminder plugin (22 lines)

**Entry Points:**
- No application entry points exist yet

**Core Logic:**
- No application code exists yet

**Testing:**
- No test infrastructure exists yet

## Naming Conventions

**Files:**
- Root config files: lowercase, no extension prefix (`opencode.json`)
- Agent instructions: `UPPERCASE.md` (`AGENTS.md`)
- Plugin files: `camelCase.js` (`graphify.js`)

**Directories:**
- Tooling directories: dot-prefixed (`.opencode/`, `.planning/`)
- Plugin directories: lowercase (`plugins/`)

## Where to Add New Code

**New Application Source:**
- No established convention yet — the project has no source code
- Recommended: Create a `src/` directory for application code once development begins
- The project name `ttsQwen` suggests Python may be the primary language (Qwen models are typically used via Python SDKs)

**New OpenCode Plugins:**
- Plugin code: `.opencode/plugins/<pluginName>.js`
- Each plugin exports an async factory function following the `@opencode-ai/plugin` API

**New GSD Planning Artifacts:**
- Phase plans: `.planning/phases/`
- Project config: `.planning/PROJECT.md`
- Roadmap: `.planning/ROADMAP.md`

**Tests:**
- No test directory established yet
- Typical Python patterns: `tests/` at project root, or `test_` prefixed files co-located with source

## Special Directories

**`.opencode/node_modules/`:**
- Purpose: npm dependencies for OpenCode plugins
- Generated: Yes (by `npm install`)
- Committed: No (gitignored)

**`graphify-out/` (referenced but does not exist yet):**
- Purpose: Will contain knowledge graph output when source code exists
- Generated: Yes (by `graphify update .`)
- Committed: Likely no (typically gitignored)

## Project State

**Current status:** Pre-development
- Git repo initialized with zero commits on `main` branch
- Only tooling infrastructure files exist
- No application source code, no dependencies, no tests
- Ready for initial project scaffolding

---

*Structure analysis: 2026-04-25*
