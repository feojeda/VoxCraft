---
phase: 03-user-accounts-complete-frontend
plan: 03
type: execute
wave: 3
depends_on:
  - "03-02"
files_modified:
  - backend/app/schemas/history.py
  - backend/app/api/routes/history.py
  - backend/app/api/routes/audio.py
  - backend/app/main.py
  - frontend/src/lib/types.ts
  - frontend/src/lib/api-client.ts
  - frontend/src/app/history/page.tsx
autonomous: true
requirements:
  - OUT-04
must_haves:
  truths:
    - "Authenticated user can view their generation history at /history"
    - "History cards show text preview, voice name used, generation date, and status"
    - "User can replay audio directly from a history card without downloading"
    - "User can re-download MP3 and WAV from a history card"
    - "User can delete a history item with a confirmation step"
    - "Audio serving verifies job ownership before returning files"
  artifacts:
    - path: "backend/app/api/routes/history.py"
      provides: "History list and delete endpoints"
      exports: ["list_history", "delete_history"]
    - path: "backend/app/schemas/history.py"
      provides: "History response schemas"
    - path: "frontend/src/app/history/page.tsx"
      provides: "History page UI"
    - path: "backend/app/api/routes/audio.py"
      provides: "Audio serving with ownership check"
  key_links:
    - from: "frontend/src/app/history/page.tsx"
      to: "/api/history"
      via: "React Query fetch"
      pattern: "apiClient\\.(listHistory|getHistory)"
    - from: "backend/app/api/routes/history.py"
      to: "backend/app/models/job.py"
      via: "filter by user_id and order by created_at desc"
      pattern: "Job\\.user_id"
    - from: "backend/app/api/routes/audio.py"
      to: "backend/app/models/job.py"
      via: "verify job ownership before FileResponse"
      pattern: "job\\.user_id"
---

<objective>
Build the generation history feature: backend API to list and delete a user's jobs, and a frontend page that displays jobs as a scannable card feed with inline audio replay, re-download, and delete confirmation.

Purpose: Users need to see what they've generated before, replay it, and download it again without regenerating.
Output: History API endpoints and a fully functional /history page.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-user-accounts-complete-frontend/03-CONTEXT.md

## Existing Interfaces

From `backend/app/schemas/tts.py`:
```python
class JobStatusResponse(BaseModel):
    id: str
    status: str
    progress: int
    text: str
    mode: str
    language: str
    speaker: str | None
    speed: float
    instruct: str | None
    instructions: str | None
    ref_audio: str | None
    ref_text: str | None
    audio_wav_path: str | None
    audio_mp3_path: str | None
    error_message: str | None
    created_at: datetime | None
    completed_at: datetime | None
```

From `backend/app/api/routes/audio.py`:
```python
@router.get("/audio/{job_id}/wav")
async def serve_wav(job_id: str) -> FileResponse:
    job = await job_manager.get_job(job_id)
    if job is None: raise HTTPException(404, ...)
    if job.status != "completed": raise HTTPException(404, ...)
    # ... serves file
```

From `frontend/src/components/audio-player.tsx`:
- Accepts `audioUrl: string | null` and `isGenerating: boolean`
- Uses WaveSurfer.js for waveform display

From `frontend/src/components/download-buttons.tsx`:
- Accepts `wavUrl: string` and `mp3Url: string`

## Locked Decisions (from 03-CONTEXT.md)
- D-11: History displayed as a vertical scrollable card list
- D-12: Each card shows first 2–3 lines of text preview, voice name used, generation date, play and download buttons
- D-13: Users can delete individual history items with a confirmation step
- D-14: Re-download of past audio files (both MP3 and WAV) supported
- D-15: Audio can be replayed directly from the history card without downloading
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend history API and audio ownership enforcement</name>
  <files>
    backend/app/schemas/history.py
    backend/app/api/routes/history.py
    backend/app/api/routes/audio.py
    backend/app/main.py
  </files>
  <action>
    1. Create `backend/app/schemas/history.py`:
       - `HistoryItemResponse` with fields: `id`, `text`, `voice_name`, `status`, `mode`, `speed`, `created_at`, `completed_at`, `audio_wav_url`, `audio_mp3_url`, `error_message`. Use `model_config = {"from_attributes": True}`.
       - `HistoryListResponse` with `items: list[HistoryItemResponse]` and `total: int`.
       - Build `audio_wav_url` and `audio_mp3_url` as relative URLs (`/api/audio/{job.id}/wav` and `/api/audio/{job.id}/mp3`) in the response model or router logic.
    2. Create `backend/app/api/routes/history.py`:
       - `GET /history` — accepts optional `skip: int = 0` and `limit: int = 20` query params.
         - Query `Job` filtered by `Job.user_id == current_user.id` and ordered by `Job.created_at.desc()`.
         - Apply offset/limit pagination.
         - Return `HistoryListResponse`.
       - `DELETE /history/{job_id}` — fetch job by ID, verify `job.user_id == current_user.id` or raise 404. Delete the job record from the database. Also delete the associated audio files from disk (WAV and MP3 paths) if they exist, to prevent orphaned files. Return 204.
       - `GET /history/{job_id}` (optional but useful) — return a single `HistoryItemResponse` for the job if owned by current user.
    3. Update `backend/app/api/routes/audio.py`:
       - Add `current_user: User = Depends(get_current_user)` to both `serve_wav` and `serve_mp3`.
       - After `job = await job_manager.get_job(job_id)`, add ownership check: `if job.user_id != current_user.id: raise HTTPException(status_code=404, detail="Job not found")`.
    4. Wire the history router into `backend/app/main.py` with `app.include_router(history_router, prefix="/api")`.
    5. Ensure `get_current_user` is imported from `app.api.deps`.
  </action>
  <verify>
    <automated>cd /Users/franciscoojeda/workspace/ttsQwen/backend && python -c "from app.main import app; [r for r in app.routes if hasattr(r, 'name') and 'history' in r.name]; print('History router mounted')"</automated>
  </verify>
  <done>History endpoints exist, filter by user, and audio routes verify ownership.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend history page with cards, replay, download, and delete</name>
  <files>
    frontend/src/lib/types.ts
    frontend/src/lib/api-client.ts
    frontend/src/app/history/page.tsx
  </files>
  <action>
    1. Update `frontend/src/lib/types.ts`:
       - Add `HistoryItem` interface matching `HistoryItemResponse`:
         ```typescript
         export interface HistoryItem {
           id: string;
           text: string;
           voice_name: string | null;
           status: JobStatus;
           mode: TTSMode;
           speed: number;
           created_at: string;
           completed_at: string | null;
           audio_wav_url: string | null;
           audio_mp3_url: string | null;
           error_message: string | null;
         }
         ```
       - Add `HistoryListResponse` interface.
    2. Update `frontend/src/lib/api-client.ts`:
       - Add `getHistory(skip?: number, limit?: number): Promise<HistoryListResponse>`
       - Add `deleteHistoryItem(jobId: string): Promise<void>`
       - Use `credentials: "include"` (already configured in request base).
    3. Create `frontend/src/app/history/page.tsx`:
       - "use client" directive.
       - Use React Query's `useQuery` to fetch history: `apiClient.getHistory(0, 20)`.
       - Use `useMutation` for delete with `queryClient.invalidateQueries({ queryKey: ["history"] })` on success.
       - Display loading state (skeleton or spinner) and empty state (reuse `EmptyState` component or create a simple message).
       - Render a vertical list of cards. Each card:
         - `bg-[var(--surface)] border border-[var(--border)] rounded-xl p-4 sm:p-5`
         - Text preview: first 2–3 lines of `item.text` (use CSS `line-clamp-2` or substring with ellipsis). Show full text on click if desired (agent's discretion).
         - Voice name badge: small pill showing `item.voice_name || "Unknown voice"` with `bg-[var(--accent)]/10 text-[var(--accent)]`.
         - Date: `new Date(item.created_at).toLocaleDateString()` in muted text.
         - Status badge: colored by status (`completed` = green, `failed` = red, `processing` = yellow, `queued` = gray).
         - For completed jobs: inline `AudioPlayer` component with `audioUrl={item.audio_wav_url}` and `isGenerating={false}`.
         - For completed jobs: `DownloadButtons` with `wavUrl={item.audio_wav_url}` and `mp3Url={item.audio_mp3_url}`.
         - Delete button: trash icon (`Trash2` from lucide) on the top-right of the card. Clicking shows a browser `confirm()` dialog: "Are you sure you want to delete this generation?". On confirm, call delete mutation.
       - Pagination: show a "Load more" button if `total > items.length` that fetches the next page and appends. Or use simple numbered pagination. Agent's discretion — simplest is offset-based "Load more".
       - Responsive: cards stack vertically on all screen sizes; padding adjusts via `sm:` breakpoints.
       - Dark theme styling consistent with existing app.
  </action>
  <verify>
    <automated>cd /Users/franciscoojeda/workspace/ttsQwen/frontend && pnpm build</automated>
  </verify>
  <done>History page compiles, fetches user's jobs, renders cards with replay/download/delete, and handles empty state.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → History API | User can only access their own jobs |
| Client → Audio API | Audio files are tied to job ownership |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Info Disclosure | Access other users' history | mitigate | GET /history filters by `user_id`; GET /history/{id} verifies ownership; audio routes verify ownership |
| T-03-12 | Tampering | Delete another user's job | mitigate | DELETE /history/{id} verifies `job.user_id == current_user.id`; returns 404 if not owned |
| T-03-13 | Repudiation | No audit log for deletes | accept | v1 scope; add audit logging in future phase if required |
</threat_model>

<verification>
1. `pnpm build` passes with zero TypeScript errors.
2. `curl http://localhost:8000/api/history -b cookies.txt` returns only jobs belonging to the authenticated user.
3. `curl http://localhost:8000/api/audio/{job_id}/wav -b cookies.txt` for another user's job returns 404.
4. History page at `/history` renders a list of cards when jobs exist.
5. Each completed card shows a working WaveSurfer player and download buttons.
6. Delete button with confirmation removes the card and invalidates the query.
7. Empty state shows when user has no history.
</verification>

<success_criteria>
- Backend history API lists only the current user's jobs with pagination
- Backend audio routes verify job ownership before serving files
- Frontend /history page renders a scrollable card list
- History cards display text preview, voice name, date, status, and inline audio player
- Re-download buttons work for both MP3 and WAV
- Delete with confirmation removes the job and refreshes the list
- `pnpm build` passes
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-accounts-complete-frontend/03-03-SUMMARY.md`
</output>
