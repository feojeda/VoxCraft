---
phase: 03-user-accounts-complete-frontend
plan: 04
type: execute
wave: 4
depends_on:
  - "03-03"
files_modified:
  - backend/app/models/preset.py
  - backend/app/models/__init__.py
  - backend/app/schemas/preset.py
  - backend/app/api/routes/presets.py
  - backend/app/main.py
  - frontend/src/lib/types.ts
  - frontend/src/lib/api-client.ts
  - frontend/src/components/voice-preset-controls.tsx
  - frontend/src/app/page.tsx
  - frontend/src/app/account/page.tsx
autonomous: true
requirements:
  - OUT-08
  - USER-04
must_haves:
  truths:
    - "User can save current generation settings as a named voice preset"
    - "User can load a saved preset from a dropdown to populate speed, emotion, and instruct"
    - "Presets are per-user and persist across browser sessions"
    - "User can view their account info at /account"
    - "Text input area displays live character and word count"
  artifacts:
    - path: "backend/app/models/preset.py"
      provides: "VoicePreset model"
      contains: "class VoicePreset"
    - path: "backend/app/api/routes/presets.py"
      provides: "Preset CRUD endpoints"
    - path: "frontend/src/components/voice-preset-controls.tsx"
      provides: "Preset save/load UI"
    - path: "frontend/src/app/account/page.tsx"
      provides: "Account page"
  key_links:
    - from: "frontend/src/components/voice-preset-controls.tsx"
      to: "frontend/src/app/page.tsx"
      via: "callback props to update generation state"
      pattern: "onChange|setSpeed|setEmotionPreset"
    - from: "backend/app/api/routes/presets.py"
      to: "backend/app/models/preset.py"
      via: "CRUD operations filtered by user"
      pattern: "VoicePreset\\.user_id"
---

<objective>
Build voice preset management (save/load speed + emotion + prosody combos) and the account page. Also verify that the existing text-input character/word count works correctly in the authenticated app context.

Purpose: Presets reduce repetitive configuration for users who frequently use the same voice settings. The account page provides a home for user-specific actions. Character/word count is a v1 requirement that needs verification.
Output: Voice preset backend and UI, account page, verified text counters.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-user-accounts-complete-frontend/03-CONTEXT.md

## Existing Interfaces

From `frontend/src/app/page.tsx`:
```tsx
const [speed, setSpeed] = useState(1.0);
const [emotionPreset, setEmotionPreset] = useState<string | null>(null);
const [instruct, setInstruct] = useState("");
// ... passed to SpeedSlider, EmotionSelector, ProsodyInput
```

From `frontend/src/components/text-input.tsx`:
```tsx
const charCount = value.length;
const wordCount = value.trim() === "" ? 0 : value.trim().split(/\s+/).length;
// displayed below textarea
```

From `frontend/src/components/speed-slider.tsx`:
- Accepts `value: number` and `onChange: (value: number) => void`

From `frontend/src/components/EmotionSelector.tsx`:
- Accepts `value: string | null` and `onSelect: (value: string | null) => void`

From `frontend/src/components/ProsodyInput.tsx`:
- Accepts `value: string` and `onChange: (value: string) => void`

## Locked Decisions (from 03-CONTEXT.md)
- D-16: Named voice presets with explicit "Save Preset" button and a dropdown to load saved presets
- D-17: Preset controls located near the voice picker on the generation page
- D-18: Each preset saves speed value, emotion preset selection, and custom prosody instruct text
- D-19: Presets are per-user and persisted to the backend
- D-20: Live character count and word count displayed below text input
- D-21: Count updates in real-time
</context>

<tasks>

<task type="auto">
  <name>Task 1: Backend VoicePreset model and API</name>
  <files>
    backend/app/models/preset.py
    backend/app/models/__init__.py
    backend/app/schemas/preset.py
    backend/app/api/routes/presets.py
    backend/app/main.py
  </files>
  <action>
    1. Create `backend/app/models/preset.py`:
       - `class VoicePreset(Base)` with `__tablename__ = "voice_presets"`
       - `id: Mapped[str] = mapped_column(String, primary_key=True, default=lambda: str(uuid4()))`
       - `name: Mapped[str] = mapped_column(String, nullable=False)`
       - `speed: Mapped[float] = mapped_column(Float, default=1.0)`
       - `emotion_preset: Mapped[str | None] = mapped_column(String, nullable=True)`
       - `instruct: Mapped[str | None] = mapped_column(String, nullable=True)`
       - `user_id: Mapped[str] = mapped_column(String, nullable=False, index=True)`
       - `created_at: Mapped[datetime] = mapped_column(default=func.now())`
    2. Export `VoicePreset` from `backend/app/models/__init__.py`.
    3. Create `backend/app/schemas/preset.py`:
       - `VoicePresetCreateRequest`: `name: str`, `speed: float`, `emotion_preset: str | None`, `instruct: str | None`
       - `VoicePresetResponse`: `id`, `name`, `speed`, `emotion_preset`, `instruct`, `created_at`
       - `VoicePresetListResponse`: `presets: list[VoicePresetResponse]`, `total: int`
       - Use `model_config = {"from_attributes": True}` where applicable.
    4. Create `backend/app/api/routes/presets.py`:
       - `GET /presets` — list all presets for `current_user`, ordered by `created_at.desc()`. Return `VoicePresetListResponse`.
       - `POST /presets` — accept `VoicePresetCreateRequest`, create `VoicePreset(user_id=current_user.id, ...)`, commit, return `VoicePresetResponse`. Reject empty name with 400.
       - `DELETE /presets/{preset_id}` — fetch preset, verify `preset.user_id == current_user.id` or raise 404, delete, return 204.
    5. Wire presets router into `backend/app/main.py`.
    6. Recreate the SQLite database so the new table exists (same as 03-01: delete `./ttsqwen.db` if no production data; `init_db()` will recreate on next startup).
  </action>
  <verify>
    <automated>cd /Users/franciscoojeda/workspace/ttsQwen/backend && python -c "from app.main import app; [r for r in app.routes if hasattr(r, 'name') and 'preset' in r.name]; print('Presets router mounted')"</automated>
  </verify>
  <done>VoicePreset model, schemas, and CRUD endpoints exist and filter by authenticated user.</done>
</task>

<task type="auto">
  <name>Task 2: Frontend preset controls on generation page</name>
  <files>
    frontend/src/lib/types.ts
    frontend/src/lib/api-client.ts
    frontend/src/components/voice-preset-controls.tsx
    frontend/src/app/page.tsx
  </files>
  <action>
    1. Update `frontend/src/lib/types.ts`:
       - Add `VoicePreset` interface: `{ id: string; name: string; speed: number; emotion_preset: string | null; instruct: string | null; created_at: string }`
    2. Update `frontend/src/lib/api-client.ts`:
       - Add `getPresets(): Promise<{ presets: VoicePreset[]; total: number }>`
       - Add `createPreset(preset: { name: string; speed: number; emotion_preset: string | null; instruct: string | null }): Promise<VoicePreset>`
       - Add `deletePreset(presetId: string): Promise<void>`
    3. Create `frontend/src/components/voice-preset-controls.tsx`:
       - "use client".
       - Props interface:
         ```typescript
         interface VoicePresetControlsProps {
           currentSpeed: number;
           currentEmotion: string | null;
           currentInstruct: string;
           onLoadPreset: (preset: { speed: number; emotion_preset: string | null; instruct: string | null }) => void;
         }
         ```
       - Use React Query `useQuery` to fetch presets list.
       - Use `useMutation` for create and delete, with invalidation of presets query on success.
       - **Save preset:**
         - A small inline form or button that opens a popover/modal. Simplest: a button "Save Preset" next to an input field for the preset name.
         - On click: call `createPreset({ name, speed: currentSpeed, emotion_preset: currentEmotion, instruct: currentInstruct })`.
         - Clear input and show a brief success state (e.g., button text changes to "Saved!" for 2 seconds).
       - **Load preset:**
         - A dropdown (`<select>` or custom) labeled "Load Preset" showing all saved preset names.
         - On change: call `onLoadPreset(preset)` with the selected preset's values.
         - Include a "Delete" icon/button next to each preset in the dropdown or as a separate action. Simplest: a small trash icon next to the load dropdown that deletes the currently selected preset.
       - Styling: compact horizontal layout, same card section as voice picker. Use `text-sm`, `bg-[var(--background)]`, `border-[var(--border)]`, `rounded-lg`.
       - Empty state: if no presets, show "No saved presets yet" in muted text.
    4. Update `frontend/src/app/page.tsx`:
       - Import `VoicePresetControls`.
       - Add it inside the `Mode & Voice` section, positioned near the voice picker (after `SpeedSlider` or after `ProsodyInput` — agent's discretion, but keep it within the `speech` mode block since presets only apply to speech mode).
       - Implement `handleLoadPreset` callback that calls `setSpeed(preset.speed)`, `setEmotionPreset(preset.emotion_preset)`, and `setInstruct(preset.instruct)`.
       - Ensure the component re-renders correctly when presets are loaded.
  </action>
  <verify>
    <automated>cd /Users/franciscoojeda/workspace/ttsQwen/frontend && pnpm build</automated>
  </verify>
  <done>Preset controls compile, can save current settings, load them back into the generation form, and delete presets.</done>
</task>

<task type="auto">
  <name>Task 3: Account page and final verification</name>
  <files>
    frontend/src/app/account/page.tsx
  </files>
  <action>
    1. Create `frontend/src/app/account/page.tsx`:
       - "use client".
       - Use `useAuth()` to get `user` and `logout`.
       - Simple card-based layout: centered, max-width ~md.
       - Sections:
         - **Profile:** Display user's email. Label it "Email" in muted text, value in primary text.
         - **Actions:** A "Log out" button that calls `logout()` and redirects to `/login`.
       - Styling consistent with the rest of the app: `bg-[var(--surface)]`, `border-[var(--border)]`, `rounded-xl`, `p-6`.
       - No password change or advanced settings (deferred per CONTEXT.md).
    2. Verify text-input character/word count (USER-04):
       - Open `frontend/src/components/text-input.tsx` and confirm that `charCount` and `wordCount` are computed and displayed.
       - The component already shows counts. Ensure the display still works correctly after layout changes (the nav bar should not break the textarea layout).
       - No code changes needed unless the layout broke something.
    3. Run a full frontend build to ensure no regressions:
       - `pnpm build` must pass with zero errors.
    4. Verify navigation links:
       - `/account` is accessible from the top nav "Account" link.
       - All protected routes (`/`, `/history`, `/voices`, `/account`) show the top nav.
       - `/login` and `/register` do NOT show the top nav (handled by TopNav returning null when unauthenticated).
  </action>
  <verify>
    <automated>cd /Users/franciscoojeda/workspace/ttsQwen/frontend && pnpm build</automated>
  </verify>
  <done>Account page compiles, shows user email and logout button, text-input counters work, and full build passes.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Client → Presets API | Presets are user-scoped; no cross-user access |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Tampering | Modify another user's preset | mitigate | All preset endpoints filter by `user_id`; delete verifies ownership |
| T-03-15 | Info Disclosure | Preset names leak info | accept | Preset names are user-created and low-sensitivity |
| T-03-16 | Denial of Service | Unlimited preset creation | accept | No rate limiting in v1; SQLite can handle thousands of rows per user |
</threat_model>

<verification>
1. `pnpm build` passes with zero TypeScript errors.
2. User can save a preset from the generation page and see it appear in the load dropdown.
3. Loading a preset updates speed slider, emotion selector, and prosody input to saved values.
4. Deleting a preset removes it from the dropdown.
5. `/account` page shows the user's email and a working logout button.
6. Text input on the generation page shows live character and word counts.
7. Navigation links work across all pages: Generate → History → Voices → Account.
</verification>

<success_criteria>
- VoicePreset backend model and CRUD API exist and are user-scoped
- Generation page has save/load/delete preset controls
- Loading a preset correctly populates speed, emotion, and instruct fields
- Account page displays user email and logout button
- Text input shows live character and word counts (USER-04 already implemented, verified working)
- Full frontend build passes without errors
- No regressions in existing Phase 1 or Phase 2 functionality
</success_criteria>

<output>
After completion, create `.planning/phases/03-user-accounts-complete-frontend/03-04-SUMMARY.md`
</output>
