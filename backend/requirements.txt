# Web framework
fastapi[standard]>=0.136
uvicorn[standard]>=0.34

# Task queue
celery[redis]>=5.6
redis>=5.0

# Database
sqlalchemy[asyncio]>=2.0
aiosqlite>=0.20
alembic>=1.13

# Validation & settings
pydantic>=2.0
pydantic-settings>=2.0

# TTS engine (HTTP client — model runs on external server)
httpx>=0.28
soundfile>=0.12

# Dev dependencies
pytest
pytest-asyncio
ruff
